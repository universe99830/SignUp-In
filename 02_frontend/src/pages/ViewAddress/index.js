import React from 'react'
import { ViewAddress as ViewAddressController } from '../../components/admin/ViewAddress'

export const ViewAddress = (props) => {
  return (
    <>
      <ViewAddressController {...props} />
    </>
  )
}
